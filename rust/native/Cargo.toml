[package]
name = "fabric-language-rust-native"
description = "JNI trampoline for fabric-language-rust: the single native library with Java-visible symbols"
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "fabric_language_rust"
crate-type = ["cdylib"]

[dependencies]
jni = { workspace = true }
libloading = { workspace = true }
